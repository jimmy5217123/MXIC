import { MainGroup, SubGroup } from './tree';
var app = new Vue({
    el: '#app',
    vuetify: new Vuetify(),
    data: {
        MainGroup,
        SubGroup
    },
    computed: {
    },
    methods: {
    }
})